// hooks/useStatusBar.ts
// Note: Status bar is not applicable in web browsers.
// This hook is kept for compatibility but does nothing in PWA.

export const useStatusBar = (): void => {
  // No-op for web - status bars don't exist in browsers
  // This hook exists only for compatibility with the React Native codebase
};

